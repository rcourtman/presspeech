{
  "project": "Presspeech",
  "privacy_model": "Local dictation, no telemetry, no cloud transcription endpoint",
  "network_calls": [
    {
      "name": "macos_first_launch_model_download",
      "when": "First launch until the model weights are cached locally; again if the local cache fails its integrity check and is re-downloaded; again when the user chooses Reset Speech Model Cache",
      "destination": "huggingface.co",
      "purpose": "FluidAudio downloads the Parakeet TDT v3 CoreML model weights",
      "approximate_size": "500-600 MB",
      "user_triggered": false,
      "can_disable": false,
      "data_sent": "Standard HTTPS request metadata; no dictation audio. If HF_TOKEN, HUGGING_FACE_HUB_TOKEN, or HUGGINGFACEHUB_API_TOKEN is set in the environment, FluidAudio attaches it as an Authorization header to huggingface.co; most users do not have these set"
    },
    {
      "name": "windows_first_launch_model_download",
      "when": "First Windows launch until the automatically selected local model is cached; again after the user selects an uncached model in Settings",
      "destination": "huggingface.co",
      "purpose": "Download local speech-model weights: Parakeet TDT v3 when NVIDIA CUDA is usable, or Whisper base.en for the automatic CPU first-run path; other models are user-selectable",
      "approximate_size": "About 2.5 GB for Parakeet TDT v3 or 141 MiB for Whisper base.en",
      "user_triggered": false,
      "can_disable": false,
      "data_sent": "Standard HTTPS request metadata; no dictation audio, transcript, settings, or usage data"
    },
    {
      "name": "macos_update_check",
      "when": "Every 6 hours when automatic update checks are enabled, plus one check shortly after launch",
      "destination": "api.github.com/repos/rcourtman/presspeech/releases/latest",
      "purpose": "Check whether a newer Presspeech release is available and notify the user",
      "user_triggered": false,
      "can_disable": true,
      "data_sent": "GET with a fixed User-Agent header, exactly presspeech-update-check; no version, device, or user identifiers, and no transcript, microphone audio, settings, or usage counts"
    },
    {
      "name": "windows_update_check",
      "when": "At most once every 24 hours when automatic checks are enabled, plus manual checks from the tray menu",
      "destination": "api.github.com/repos/rcourtman/presspeech/releases?per_page=100",
      "purpose": "Find a newer complete windows-vX.Y.Z release and offer it to the user",
      "user_triggered": false,
      "can_disable": true,
      "data_sent": "GET with a fixed User-Agent header, exactly presspeech-windows-update-check; no version, device, or user identifiers, and no transcript, microphone audio, settings, or usage counts"
    },
    {
      "name": "user_triggered_install_or_update",
      "when": "Only when the user downloads the app directly, installs through Homebrew, or chooses to update through the menu or Homebrew",
      "destination": [
        "github.com/rcourtman/presspeech/releases/download/...",
        "github.com/rcourtman/homebrew-presspeech",
        "formulae.brew.sh",
        "Homebrew's own update endpoints (github.com / api.github.com)"
      ],
      "purpose": "User-triggered install/update flow: direct release download, a Homebrew metadata refresh followed by the signed macOS archive, or a Windows installer whose published size and SHA-256 are verified before launch",
      "user_triggered": true,
      "can_disable": true,
      "data_sent": "Browser or Homebrew request metadata; no dictation audio"
    }
  ],
  "local_storage": {
    "preferences": "~/Library/Preferences/com.local.presspeech.plist",
    "logs": "~/Library/Logs/Presspeech.log",
    "model_cache": "~/Library/Application Support/FluidAudio/",
    "windows_preferences": "%APPDATA%\\Presspeech\\config.json",
    "windows_logs": "%APPDATA%\\Presspeech\\log.txt",
    "windows_model_cache": "%USERPROFILE%\\.cache\\huggingface",
    "transcripts": "macOS recent transcript history is configurable, stays in memory only, and clears on quit; Windows does not keep an in-app transcript history",
    "clipboard": "On macOS, pasted transcripts stay on the system pasteboard by default; users can opt into guarded best-effort restoration of the previous clipboard, which is skipped if the snapshot is incomplete or another process has copied newer content. On Windows, the transcript remains on the clipboard until overwritten"
  }
}
